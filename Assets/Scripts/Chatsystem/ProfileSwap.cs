using UnityEngine;

public class ProfileSwap : MonoBehaviour
{
    [Header("????????????????")]
    public RectTransform profile1;   // ???????? 1
    public RectTransform profile2;   // ???????? 2

    public void Swap()
    {
        // ??????????? anchoredPosition ?????????
        Vector2 temp = profile1.anchoredPosition;
        profile1.anchoredPosition = profile2.anchoredPosition;
        profile2.anchoredPosition = temp;
    }
}
